# Copy this file to .env and fill in the values. .env is NEVER committed.

# Public host of the tool
PUBLIC_HOST=analytics.example.com

# ClickHouse (strong password: openssl rand -hex 24)
CLICKHOUSE_PASSWORD=change_me

# Dashboard auth (login + 2FA)
AUTH_SECRET=change_me_openssl_rand_hex_32
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=change_me_strong_password

# Visitor country geolocation:
#   cloudflare = free, via the CF-IPCountry header (orange-proxied subdomain). Recommended.
#   maxmind    = via GeoLite2 (also enables city). Requires the two keys below.
#   off        = no country
GEO_MODE=cloudflare
MAXMIND_ACCOUNT_ID=
MAXMIND_LICENSE_KEY=

# Mapbox public token (URL-restricted to your domain) for the live map
MAPBOX_TOKEN=
